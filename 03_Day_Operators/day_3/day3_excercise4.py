# Declaring variables
base = input("What is the base of the triangle?: ")
height = input("What is the height?: ")

# Calculate the area
area = 0.5 * float(base) * float(height)
print("The area of the trinagle is", area, "m**2")